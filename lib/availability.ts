export const updateAvailability = async (
  email: string,
  day: string,
  status: string,
): Promise<string> => {
  console.log(`updating availability for ${email} for ${day} to ${status}.`);
  return await "";
};
